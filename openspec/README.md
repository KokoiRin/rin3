# OpenSpec 遗留说明

本目录保存 2026 年 7 月已经完成的规格和 change archive，用于迁移业务语义与架构决策，不再作为 RIN III 的当前需求流程。

- 不再创建新的 OpenSpec change；
- 当前业务行为、关键函数和测试关系以 `harness/behaviors/registry.json` 为准；
- OpenSpec 中仍有效的长期规则迁入行为数据库或架构文档后，再删除对应遗留文件；
- Git 历史已经保存实施过程，不需要永久保留纯任务流水账。
