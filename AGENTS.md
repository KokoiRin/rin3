# 协作约定

- 默认简短回答：先讲结论和最关键判断，重要内容讲清楚，不主动展开不必要的背景、例子或延伸分析。
- 验证按改动风险分级：纯内部重构且不改变行为时，默认只运行针对性测试、类型检查和 lint；不要重复运行多套等价的完整构建。
- 只有本轮修改了 UI 交互、正在修复交互回归、用户明确要求，或浏览器验证能覆盖自动化测试无法覆盖的明确风险时，才运行浏览器测试。不要仅为普通重构临时下载或引入新的浏览器测试基础设施；确有长期价值时，先说明额外成本并征得用户同意。
- 每次修改代码或内容后，会话结束前提供一个已经验证可访问的本地预览链接，方便用户立即验收。
- 如果本轮修改已经部署到 GitHub Pages 或其他远程网页，同时提供对应的远程网页链接；不要只提供 GitHub 仓库链接。

## 临时文件与生成产物

- Agent 的中间脚本、截图、音频、渲染帧、检查日志和一次性缓存默认放入系统临时目录，或已忽略的 `/work/`；不要在仓库根目录新建 `tmp/`、会话文件或其他临时目录。
- 需要交给用户但不应进入 Git 的视频、PPT、压缩包等大文件放入已忽略的 `/output/`。只有用户明确要求版本化时才加入 Git。
- 会话结束前清理本轮创建的临时文件；不要删除来源不明或可能仍有交付价值的既有文件。

## 业务行为数据库强制约束

- `harness/` 是独立验收 package，只能单向读取、构建和测试项目；`app/`、`components/`、`lib/`、产品 `package.json` 和公开静态产物不得反向依赖 Harness。
- `harness/behaviors/registry.json` 是业务行为、关键函数和测试关系的结构化事实源；长期架构约束写入 `docs/architecture.md` 或 ADR，不再使用 OpenSpec change。
- 修改 `app/`、`components/`、`lib/`、`packages/rin-document/src/` 或 `harness/scripts/` 下的代码前，先运行 `npm --prefix harness run behavior -- impact <函数名或文件>` 查看受影响业务；查不到时必须判断是新增行为、遗漏映射还是纯基础设施。
- 每次修改代码后必须同步更新受影响行为：提升 `version`、更新 `lastReviewedOn`，并复核中文业务描述、代码符号和测试映射；新增可观察行为必须创建新的稳定 ID。
- 纳入数据库的测试名称必须使用 `[行为ID] 中文业务结果`，并在数据库中为每个测试保留一句非技术化的中文业务行为说明。
- 新增或修改业务测试时必须把它纳入行为数据库；尚未带行为 ID 的旧测试属于待迁移基线，不得继续新增无 ID 的业务测试。
- 完成修改前至少运行对应行为、`npm --prefix harness run check` 和按风险选择的项目验证；多个受影响行为使用一次 `npm --prefix harness run behavior -- run <行为ID1> <行为ID2> ...`，共享同一次生产构建，不要逐条重复构建。
- 浏览器行为会在构建前检查 Harness 对应版本的 Chromium。缺失时只运行一次 `npm --prefix harness run install:browser`，不要在仓库根目录执行其他版本的 Playwright 安装命令。
